package com.yams.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // TODO: configurer Spring Security (authentification, JWT, roles...)
}
